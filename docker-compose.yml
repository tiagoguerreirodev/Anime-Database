version: '3.7'
services:
  # api:
  #   image: python:3.10-slim-bullseye
  #   command: sh -c "python3 -m pip install fastapi pydantic requests email-validator && python3 main.py"
  #   working_dir: /api
  #   container_name: backend
  #   volumes:
  #     - ./AnimeAPI:/api
  #   ports:
  #     - 5000:5000
  front:
    image: node:18-alpine
    working_dir: /front
    command: sh -c "npm install && npm start"
    container_name: frontend
    volumes:
      - ./AnimeFront:/front
      - /front/node_modules
    ports:
      - 80:80